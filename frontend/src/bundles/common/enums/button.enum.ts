enum ButtonType {
    BUTTON = 'button',
    SUBMIT = 'submit',
}

enum ButtonVariant {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    PLAIN = 'plain',
    ROUND = 'round',
}

enum ButtonSize {
    MEDIUM = 'medium',
    SMALL = 'small',
}

export { ButtonSize, ButtonType, ButtonVariant };
