import React from 'react';

import { Tabs } from '../components/tabs/tabs';

const Base: React.FC = () => {
    return (
        <div>
            Base Page
            <Tabs />
        </div>
    );
};

export { Base };
