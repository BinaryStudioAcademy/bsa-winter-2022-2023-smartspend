import React from 'react';

import { CreateInputNote } from '../components/input/app-input';

const Base: React.FC = () => {
    return (
        <div>
            Base Page
            <CreateInputNote />
        </div>
    );
};

export { Base };
